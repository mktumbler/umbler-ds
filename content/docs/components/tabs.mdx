---
title: Tabs
description: Navegação entre painéis de conteúdo relacionados. Construído sobre Radix Tabs, com suporte completo a teclado e ARIA.
---

import { TabsBasic, TabsWithIcons, TabsDisabled } from '@/components/demos/tabs-variants';
import { VariantGrid, VariantCard } from '@/components/docs/variant-grid';

Use `Tabs` para alternar entre seções **paralelas** de um mesmo contexto — quando o usuário não precisa ver tudo ao mesmo tempo, mas pode querer comparar rapidamente. Evite tabs para fluxos sequenciais (use steps/wizard) ou para o que cabe simultaneamente na tela (use seções com headings).

## Variantes

<VariantGrid columns={1}>
  <VariantCard label="Básico" hint="3 tabs com conteúdo simples — padrão segmented control" minHeight={180}>
    <TabsBasic />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Com ícones" hint="Phosphor icons + conteúdo contextual por tab" minHeight={220}>
    <TabsWithIcons />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Tab desabilitado" hint="disabled no TabsTrigger — não interativo, opacity reduzida" minHeight={180}>
    <TabsDisabled />
  </VariantCard>
</VariantGrid>

## Anatomia

```tsx
<Tabs defaultValue="a">
  <TabsList>
    <TabsTrigger value="a">A</TabsTrigger>
    <TabsTrigger value="b">B</TabsTrigger>
  </TabsList>
  <TabsContent value="a">Painel A</TabsContent>
  <TabsContent value="b">Painel B</TabsContent>
</Tabs>
```

## Acessibilidade

- ↔ setas navegam entre triggers; `Home`/`End` vão para primeiro/último.
- `Enter` ou `Space` ativam o tab focado (automático por padrão; controle via `activationMode="manual"`).
- A relação trigger ↔ painel é exposta como `role="tab"` / `role="tabpanel"` com `aria-controls`/`aria-labelledby` automáticos.

## Controlado

```tsx
const [tab, setTab] = useState('overview');

<Tabs value={tab} onValueChange={setTab}>
  ...
</Tabs>
```

## Underline tabs

O `TabsList` por padrão é um *segmented control*. Para o visual de *underline tabs*, sobrescreva `className`:

```tsx
<TabsList className="border-0 border-b border-border bg-transparent rounded-none p-0">
  <TabsTrigger
    value="a"
    className="rounded-none border-b-2 border-transparent data-[state=active]:border-brand-500 data-[state=active]:bg-transparent"
  >
    ...
  </TabsTrigger>
</TabsList>
```
