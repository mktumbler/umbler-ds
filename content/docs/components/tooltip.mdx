---
title: Tooltip
description: Texto auxiliar não-intrusivo exibido no hover ou focus de um elemento. Construído sobre Radix Tooltip, acessível e com posicionamento automático.
---

import { TooltipBasic, TooltipSides, TooltipRich } from '@/components/demos/tooltip-variants';
import { VariantGrid, VariantCard } from '@/components/docs/variant-grid';

Use `Tooltip` para descrever ações abreviadas (ícones, botões sem rótulo) ou explicar um termo. Mantenha o texto curto — para conteúdo rico ou clicável, prefira `Popover`.

Envolva sua árvore (ou o trecho que usa tooltips) em `TooltipProvider` uma única vez.

## Variantes

<VariantGrid columns={1}>
  <VariantCard label="Básico" hint="Hover ou focus no botão — texto curto descritivo" minHeight={100}>
    <TooltipBasic />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Posicionamento — 4 lados" hint="side: top, right, bottom, left — Radix reposiciona se faltar viewport" minHeight={120}>
    <TooltipSides />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Conteúdo rico" hint="Aceita qualquer ReactNode — título + descrição" minHeight={100}>
    <TooltipRich />
  </VariantCard>
</VariantGrid>

## Acessibilidade

- `TooltipTrigger` deve envolver um elemento focusável (botão, link). Use `asChild` para herdar o elemento filho ao invés de inserir um wrapper.
- O tooltip abre no `hover` e no `focus` do trigger, e fecha no `blur` ou `Escape`.
- Para um ícone-only, forneça `aria-label` no botão — o texto do tooltip não é exposto como nome acessível.

## Uso

```tsx
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from '@/components/ui/tooltip';

<TooltipProvider delayDuration={150}>
  <Tooltip>
    <TooltipTrigger asChild>
      <Button variant="ghost" aria-label="Editar">
        <PencilSimple size={16} />
      </Button>
    </TooltipTrigger>
    <TooltipContent side="top">Editar item</TooltipContent>
  </Tooltip>
</TooltipProvider>
```
