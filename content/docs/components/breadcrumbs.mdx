---
title: Breadcrumbs
description: Trilha de navegação hierárquica indicando a localização atual dentro da estrutura do site/produto.
---

import { BreadcrumbsBasic, BreadcrumbsCustomSeparator, BreadcrumbsWithIcon } from '@/components/demos/breadcrumbs-variants';
import { VariantGrid, VariantCard } from '@/components/docs/variant-grid';

Compõe um `<nav>` com `<ol>` semântico. Cada item é um link; o último (página atual) é texto com `aria-current="page"`.

## Variantes

<VariantGrid columns={1}>
  <VariantCard label="Básico" hint="Chevron padrão — BreadcrumbSeparator sem children">
    <BreadcrumbsBasic />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Separador customizado" hint="BreadcrumbSeparator aceita qualquer ReactNode como children">
    <BreadcrumbsCustomSeparator />
  </VariantCard>
</VariantGrid>

<VariantGrid columns={1}>
  <VariantCard label="Item com ícone" hint="Primeiro nível como ícone de casa — forneça aria-label no item">
    <BreadcrumbsWithIcon />
  </VariantCard>
</VariantGrid>

## Acessibilidade

- `<nav aria-label="Trilha de navegação">` agrupa a trilha.
- `<ol>` indica ordem hierárquica.
- Último item usa `aria-current="page"` no lugar de `<a>` — leitores anunciam corretamente.

## Uso

```tsx
import {
  Breadcrumbs,
  BreadcrumbItem,
  BreadcrumbCurrent,
  BreadcrumbSeparator,
} from '@/components/ui/breadcrumbs';

<Breadcrumbs>
  <BreadcrumbItem href="/">Início</BreadcrumbItem>
  <BreadcrumbSeparator />
  <BreadcrumbItem href="/docs">Docs</BreadcrumbItem>
  <BreadcrumbSeparator />
  <BreadcrumbCurrent>Página atual</BreadcrumbCurrent>
</Breadcrumbs>
```
