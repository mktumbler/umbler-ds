---
title: Spinner
description: Indicador de carregamento com dois pontos orbitando — estilo do loader Umbler. Também disponível em variante arco para uso inline em botões.
---

import { SpinnerOrbitSizes, SpinnerOrbitColors, SpinnerArc } from '@/components/demos/spinner-variants';

O `Spinner` replica o loader oficial Umbler: dois pontos (azul `#8BBCF8` e salmão `#F8C4B8`) em órbita. A variante `arc` mantém o arco giratório clássico para contextos compactos (botões, badges).

## Orbit — tamanhos

<ComponentPreview title="Orbit — sm / md / lg / xl">
  <SpinnerOrbitSizes />
</ComponentPreview>

## Orbit — cores customizáveis

Use `colorA` e `colorB` para adaptar às cores do contexto.

<ComponentPreview title="Cores">
  <SpinnerOrbitColors />
</ComponentPreview>

## Arc — inline em botões

Variante `arc` usa `currentColor` — controle pela classe `text-*` do pai.

<ComponentPreview title="Arc (variant='arc')">
  <SpinnerArc />
</ComponentPreview>

## Props

<TokenTable
  columns={['Prop', 'Tipo', 'Default', 'Descrição']}
  rows={[
    ['size', '"sm" | "md" | "lg" | "xl"', '"md"', 'Tamanho: 14 / 16 / 20 / 24px'],
    ['variant', '"orbit" | "arc"', '"orbit"', 'orbit = dois pontos (padrão Umbler), arc = arco giratório'],
    ['colorA', 'string', '"#8BBCF8"', 'Cor do ponto A (azul). Qualquer valor CSS.'],
    ['colorB', 'string', '"#F8C4B8"', 'Cor do ponto B (salmão). Qualquer valor CSS.'],
    ['className', 'string', '—', 'Classes extras'],
  ]}
/>

## Uso

```tsx
import { Spinner } from '@/components/ui/spinner';

// Padrão — orbit com cores do gradiente Umbler
<Spinner />

// Orbit com cores customizadas
<Spinner colorA="#1A5CFF" colorB="#5B8EFF" />

// Arc inline em botão
<button className="text-brand-400">
  <Spinner size="sm" variant="arc" />
  Carregando...
</button>
```
